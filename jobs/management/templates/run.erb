#!/bin/bash
set -e # exit immediately if a simple command exits with a non-zero status
set -u # report the usage of uninitialized variables

echo "errand run management update rke"
/var/vcap/jobs/management/bin/post-deploy

echo "errand run update rke  is complete"
